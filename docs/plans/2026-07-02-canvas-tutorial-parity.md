# 无限画布 × LibTV 教程全功能对齐计划（2026-07-02）

> 依据：`~/canvas_ref/feishu-tutorial-gap-analysis-2026-07-01.md`（飞书官方教程 1095 行全文分析）
> + `~/canvas_ref/libtv-full-clone-spec-2026-07-01.md`（登录态实测 55 截图）。
> 目标：教程教的完整工作流在我方 /zh/canvas 从头走到尾走得通。
> User 指令：教程功能全部写进来（2026-07-01 拍板）。

## 现况基线（已上 prod, 85c2678）

LibTV 实测 token reskin + 浮动配置面板 + 8 种节点（图/视频/文本/角色/脚本/音频/导演台/分组）
+ 15 预设配方 + 720 全景一条龙 + 导演台全套 + 成组持久化 + 快捷键/多选/右键。

## P0 — 主线断点（教程工作流走通的最小集）

| # | 功能 | 实作面 | 备注 |
|---|---|---|---|
| P0-1 | Slash 配方一波补齐（故事板/调度故事板/画面推演±N秒/光影矫正/角色·场景设定图/人像调节/情绪调节；输入框「/」唤起） | 纯前端 | 纯 prompt 模板，扩充 canvas-recipes.ts + / 触发 |
| P0-2 | 档案拖入画布（图片/视频文件 drop → 自动建节点+上传） | 纯前端 + 现有 upload-reference API | onDrop + screenToFlowPosition |
| P0-3 | 文本节点 LLM 生成（写作助手：扩写/改写） | 复用 CANVAS_STORYBOARD 同族 task spine（确认可否共用，否则走 6 登记点新增 CANVAS_TEXT） | CLAUDE.md 铁则：不可同步直连 LLM |
| P0-4 | 脚本节点 v2 第一层：shot 表格可编辑（增删/调序/改字段）+ 批量范围选择 + 批量生视频（不只生图） | 前端为主（shots 已在 node data） | 资产卡抽屉留 P2 |

## P1 — 高频工具

| # | 功能 | 实作面 |
|---|---|---|
| P1-1 | 图像基础编辑 6 件套：高清放大/扩图/重绘/擦除/抠图/裁剪（各生成子节点） | AtlasCloud /edit slug 已通（8cea8fa）；裁剪/旋转镜像纯前端 canvas |
| P1-2 | 宫格切分 + 分镜组（宫格布局 + 拼接导出） | 前端 canvas 切图 + ⌥G 合并分镜组 |
| P1-3 | 工作流保存（用户自建模板）+ 整组执行 | 前端 + Canvas 表扩展（或新 template 表） |
| P1-4 | 运镜面板升级（23 预设 + 收藏/自定义「我的运镜」）+ 摄像机 builder（9 机身×10 镜头） | 纯前端（prompt 组装） |
| P1-5 | 提示词优化按钮 | 同 P0-3 的 LLM 通道 |
| P1-6 | 历史记录 modal + 资产管理抽屉（画布图层大纲） | 前端接现有 runs API；资产库地基另计 |

## P2 — 大件

- 脚本 v2 资产卡抽屉：角色/场景/道具自动提取 + 一键生成 reference sheet + @ 引用自动连线 + 最终提示词合成（= LibTV 差异化核心，需 LLM 抽取 task）
- 视频合成节点 + 编辑台（多轨时间轴/变速/导出，FFmpeg server-side，新 task type）
- 视频解析（视频→分镜表）、人声分离、首尾帧截取、视频剪辑/高清
- 导演台下沉节点卡（内嵌 3D 预览 + 四方位/十二方位截图 + 构图参考线）+ 全景 viewer 多视角截图
- 个人资产库（保存到我的资产 / 分类 / 批量）+ 多画布 per 项目

## P3 — 需新模型/外部能力（逐项评估再做）

人像/情绪调节专用效果、自定义风格模板训练、视频高清放大、可灵主体库、智能擦除字幕、协作跟随、分享链接。

## 方法论 → 配方（随 P0-1 一起注入）

教程 12 条方法论直接变预设：「首先/然后/最后」运镜秩序句式、打光 prompt 句式、
先精修单镜再批量、分镜图作视频参考、标注修图守则、音色克隆 SOP 等
（详见 gap-analysis 报告 D 节）。

## 约束

- 模型清单只接已授权 provider（Seedance 全变体 + 既有），UI 结构对齐、内容自产。
- 所有 LLM/生成一律走 createRun→worker spine，禁止同步直连（CLAUDE.md §3）。
- 新 task type 必须走 6 登记点 SOP（types/intent/task-policy/catalog/queues/usage-categorizer）。

## 进度

- [ ] P0-1 Slash 配方
- [ ] P0-2 拖档入画布
- [ ] P0-3 文本节点 LLM
- [ ] P0-4 脚本表格 v2 第一层
