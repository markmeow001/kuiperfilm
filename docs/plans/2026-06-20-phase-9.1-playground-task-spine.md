# Phase 9.1 — Playground → Task spine（2026-06-20）

> 收斂佇列 #6（`docs/plans/2026-06-12-track-consolidation.md`）。
> 目標：把 Playground（獨立文生圖 / 圖生視頻）從 bespoke 路徑搬到統一
> `submitTask → createRun → worker(withTaskLifecycle) → run-runtime` 主幹，
> 刪掉 PR #6 的 billing sidecar。多人 + 計費統一 spine。

## 拍板決策（user 2026-06-20，A+A）

- **projectId**：用合成哨兵 `'playground'`。`Task.projectId` 是 NOT NULL 但
  **無 FK 指向 Project**（已查證 schema：只有 `user @relation`），且
  `billing/reporting.ts:VIRTUAL_PROJECT_IDS` 早已白名單 `'playground'`
  （skips UsageCost，仍寫 BalanceTransaction）。→ **零 schema 改動、零 migration**。
- **PlaygroundRun 表**：Task 變唯一真相。新任務只寫 Task；既有 PlaygroundRun
  row 凍結成 9.1 前唯讀歷史（**不雙寫、不 migrate、不刪表**）。前端歷史改讀 Task。

被否決：projectId nullable（要 migration）/ 每用戶虛擬專案（要 backfill）/
PlaygroundRun 當投影（踩 `no-multiple-sources-of-truth` guard）。

## 已驗證契約（讀過真實 code）

- `submitTask`（`src/lib/task/submitter.ts:94`）一手包辦：rate-limit → skill
  constraint → `buildDefaultTaskBillingInfo` → `createTask` →（`isAiTaskType`）
  `createRun`+`attachTaskToRun`+把 runId 蓋回 payload → `prepareTaskBilling`
  （凍結，餘額不足丟 `InsufficientBalanceError`→402）→ `publishTaskEvent(CREATED)`
  → `addTaskJob`（enqueue 失敗會 rollback billing + markTaskFailed）。回
  `{taskId, runId, status, deduped}`。
- `isAiTaskType`（`run-runtime/workflow.ts`）= 全部 TASK_TYPE；
  `workflowTypeFromTaskType` = identity。→ **註冊進 TASK_TYPE 即自動建 run，零 workflow 接線**。
- `withTaskLifecycle`（`src/lib/workers/shared.ts:218`）：mark processing → 跑
  handler 收 `result` + textUsage →（billable）`settleTaskBilling`（capture）+
  `updateTaskBillingInfo` → mark completed(result) + COMPLETED event；錯誤
  → `rollbackTaskBilling`。→ **handler 只要回 `{resultUrls:[cosKey]}`，計費/狀態/事件全自動**。
- 計費政策（`src/lib/billing/task-policy.ts`）：`buildImageTaskInfo` 讀
  `payload.imageModel|modelId|model` + `resolution`；`buildVideoTaskInfo` 讀
  `payload.videoModel|modelId|model` + `resolution` + `duration`。→ payload 帶
  `modelId`（resolveModelSelection 解出的 bare id）+ `resolution` + `duration` 即可重用。
- tasktype-coverage guard（`scripts/guards/test-tasktype-coverage-guard.mjs`）：每個
  TASK_TYPE key 必須在 `tests/contracts/task-type-catalog.ts` 有 `[TASK_TYPE.X]` owner。

## 執行切片（TDD，每片自成 commit、跑窄測試 + 相關 guard）

### Slice 1 — worker 側 spine 接線（cohesive，因 guard 耦合）
- `src/lib/task/types.ts`：TASK_TYPE 加 `PLAYGROUND_IMAGE`/`PLAYGROUND_VIDEO`。
- `tests/contracts/task-type-catalog.ts`：兩個 owner →
  `tests/unit/worker/playground-task-handler.test.ts`。
- `src/lib/billing/task-policy.ts`：兩型加進 BILLABLE_TASK_TYPES +
  `buildDefaultTaskBillingInfo` switch（image→buildImageTaskInfo、video→buildVideoTaskInfo）。
- 新 handler（thin，回 `{resultUrls}`，包進 withTaskLifecycle）：
  改寫 `playground-image.ts` / `playground-video.ts` 讀 `task.payload`、刪手動
  PlaygroundRun/billing；worker switch（`image.worker.ts`/`video.worker.ts`）加
  case、移除 `if type==='playground_*'` bespoke 分支 + 移除 union 型別。
- tests：`playground-task-handler.test.ts`（handler 回 resultUrls）+
  `playground-billing-policy.test.ts`（兩型 billingInfo 正確）。

### Slice 2 — route + 讀取側 + 刪 sidecar
- `src/app/api/playground/run/route.ts`：改成單一 `submitTask({projectId:'playground',
  type:PLAYGROUND_IMAGE|VIDEO, payload:{prompt, modelKey, modelId:resolvedModelId,
  referenceImages, referenceVideos, referenceText, resolution, aspectRatio, duration,
  outputType, generationCount, workspaceId}})`；刪 quote/freeze/PlaygroundRun.create。
- `GET /api/playground/runs` + `/runs/[runId]`：改讀 Task（projectId='playground' +
  userId）映射成既有 client run 形狀。
- 刪 `src/lib/playground/enqueue.ts` + `src/lib/playground/billing.ts`。
- `estimate-cost/route.ts`：改用 `buildDefaultTaskBillingInfo` 或保留 calcImage/calcVideo。
- 前端 query hook（`playground-mutations.ts` / runs 讀取）對齊 Task 回傳形狀。
- tests：route 整合測試（submitTask 被呼叫、402 路徑、enqueue 失敗 rollback）。

### Slice 3 — 收尾
- `npm run test:regression` 全綠（或既有紅燈登記）。
- 更新 master plan + track-consolidation #6 → ✅。
- `docs/ai-runtime/08-open-gaps.md` 記未盡事項（舊 PlaygroundRun 歷史前端是否要獨立檢視）。

## 既有紅燈（非本任務造成，先記）
- `check:test-route-coverage`：PR #5 的 `/api/skills/*`、`/api/skill-installations/*`
  沒登記進 `tests/contracts/route-catalog.ts`，`test:guards` 在它這一關就紅。
